//Infected Hospital Game (MVC Design)
src/
├── model/
│   ├── Player.java              // Manages player state: health, inventory, currentRoom, activePuzzle
│   ├── Inventory.java           // Holds and manages player's items: add, remove, check
│   ├── Item.java                // Defines items (name, type, effect, description)
│   ├── Room.java                // Represents a room: name, description, exits, monsters, items, puzzle
│   ├── Monster.java             // Defines enemy entities with health, damage, and behavior
│   └── Puzzle.java              // Represents puzzle challenges: answer, hint, result, solved state
│
├── controller/
│   ├── GameController.java      // Main game logic: movement, command parsing, flow control
│   ├── PuzzleController.java    // Handles puzzle interactions: solve, hint, leave
│   ├── CombatController.java    // Manages combat between player and monster: attack, flee, health
│   └── ItemController.java      // Manages item-related logic: use, drop, equip, inspect
│
├── view/
│   ├── GameView.java            // Displays general UI: room details, inventory, player status
│   ├── PuzzleView.java          // Outputs puzzle-related messages and prompts
│   └── CombatView.java          // Outputs combat-related information and status updates
│
├── loader/
│   └── FileLoader.java          // Loads data from external files: puzzles, rooms, items, monsters
│
├── main/
│   └── Main.java                // Game entry point: sets up the game world, launches GameController
│
└── GameStructure.java           // 📘 Developer reference for all classes and how they interact
